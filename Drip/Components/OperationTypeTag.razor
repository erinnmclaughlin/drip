@using Microsoft.OpenApi.Extensions

<span class="tag @OperationType @OperationType.ToString().ToLower()">
    @(OperationType == OperationType.Delete ? "DEL" : OperationType.GetDisplayName())
</span>

@code {
    [Parameter, EditorRequired]
    public required OperationType OperationType { get; set; }
}